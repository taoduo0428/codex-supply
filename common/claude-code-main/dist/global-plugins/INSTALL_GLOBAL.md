See repository root `INSTALL_GLOBAL.md` for full instructions.

Quick run:

```powershell
powershell -ExecutionPolicy Bypass -File .\dist\global-plugins\install_global.ps1 -WorkspacePath "C:\path\to\workspace"
```
