# Copy to "$HOME/.agents/automation/.env" and fill secrets.
# chmod 600 "$HOME/.agents/automation/.env"

# GitHub MCP auth (set one)
GITHUB_TOKEN=
# GITHUB_PAT=

# Optional explicit codex binary override for automation scripts
# CODEX_BIN="$HOME/.local/bin/codex"
# Example (VS Code extension fallback):
# CODEX_BIN="$HOME/.vscode-server/extensions/openai.chatgpt-<VERSION>-linux-x64/bin/linux-x86_64/codex"
